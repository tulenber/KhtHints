{
    "khtHintsConfigs": [
        {
            "hintName" : "LoadingSceneHint",
            "usedGroupName": "default",
            "strategy" : 1,
            "updateTimeout" : 0
        }
    ],
    "khtHintsGroupsData": [
        {
            "groupName" : "default",
            "hintsList": [
                "Default - group for the default hints.",
                "Hint without name or config will show hints from the default group.",
                "Strategy - 0:Random, 1:Sequential",
                "UpdateTimeout - update hint every N seconds, 0:no update"

            ]
        },
        {
            "groupName": "custom",
            "hintsList": [
                "Custom groups can be created.",
                "To use custom group use their name in usedGroupName field from hint config block."
            ]
        }
    ]
}